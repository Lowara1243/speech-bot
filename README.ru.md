[English](README.md) | [Русский](README.ru.md)
---

# Telegram Speech-to-Text Bot

<p align="center">
  <a href="https://github.com/Lowara1243/speech-bot/actions/workflows/ci.yml"><img alt="Статус CI" src="https://github.com/Lowara1243/speech-bot/actions/workflows/ci.yml/badge.svg"></a>
  <a href="https://github.com/Lowara1243/speech-bot/blob/main/LICENSE"><img alt="Лицензия" src="https://img.shields.io/github/license/Lowara1243/speech-bot"></a>
  <a href="https://www.python.org/downloads/"><img src="https://img.shields.io/badge/python-3.10+-blue.svg" alt="Версия Python"></a>
  <a href="https://github.com/astral-sh/ruff"><img src="https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/astral-sh/ruff/main/assets/badge/v2.json" alt="Форматирование с Ruff"></a>
</p>

Мощный Telegram бот, использующий OpenAI Whisper для преобразования аудиофайлов и голосовых сообщений в текст. Создан с использованием Aiogram 3, асинхронной обработки и упакован для легкого развертывания с помощью Docker.

---

## Содержание
- [Ключевые особенности](#-ключевые-особенности)
- [Быстрый старт (Docker Compose)](#-быстрый-старт-docker-compose)
- [Установка из исходного кода](#-установка-из-исходного-кода)
- [Конфигурация](#-конфигурация)
- [Управление ботом](#-управление-ботом)
- [Лицензия](#-лицензия)

## ✨ Ключевые особенности

-   **🎙️ Высококачественная транскрибация:** Использует модель Whisper от OpenAI для точного преобразования речи в текст.
-   **⚡️ Асинхронность:** Построен на `asyncio` и `aiogram` для высокой производительности.
-   **💾 Постоянное хранилище:** Использует SQLite для хранения данных пользователей и лимитов использования.
-   **⚙️ Гибкая система лимитов:** Настраивайте дневные, недельные или месячные лимиты использования (в секундах).
-   **🐳 Готовность к Docker:** Оптимизирован для развертывания одной командой с помощью Docker Compose.
-   **🔧 Современные инструменты:** Использует `uv` для управления пакетами и `ruff` для линтинга.

## 🚀 Быстрый старт (Docker Compose)

Это рекомендуемый способ запуска бота для производственного использования.

1.  **Создайте директорию проекта:**
    Создайте новую директорию и перейдите в нее.

2.  **Создайте `docker-compose.yml`:**
    ```yaml
    services:
      speech-bot:
        image: lowara1243/speech-bot:1.1.0
        container_name: telegram_speech_bot
        restart: always
        env_file:
          - .env
        volumes:
          - ./data:/app/data
        tty: true
    ```

3.  **Настройте бота:**
    Создайте файл `.env` в той же директории. Как минимум, укажите ваши `BOT_TOKEN` и `ADMIN_ID`.

4.  **Запустите бота:**
    ```bash
    docker-compose up -d
    ```

## 🛠️ Установка из исходного кода

<details>
<summary>Нажмите, чтобы развернуть инструкции по ручной установке</summary>

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Lowara1243/speech-bot.git
    cd speech-bot
    ```

2.  **Установите зависимости:**
    Вам потребуется установленный в системе `ffmpeg`.
    -   **Ubuntu/Debian:** `sudo apt-get install ffmpeg`
    -   **macOS:** `brew install ffmpeg`

    # Создаст .venv и установит все зависимости из pyproject.toml
    uv sync
    ```

3.  **Настройте бота:**
    ```bash
    cp .env.example .env
    nano .env
    ```

4.  **Запустите бота:**
    ```bash
    python -m src.speech_bot.main
    ```
    </details>

## ⚙️ Конфигурация

| Переменная             | Описание                                                                        | Обязательно |
|------------------------|---------------------------------------------------------------------------------|:-----------:|
| `BOT_TOKEN`            | Ваш токен Telegram бота от [@BotFather](https://t.me/BotFather).                |   **Да**    |
| `ADMIN_ID`             | Ваш личный Telegram User ID (у админа нет лимитов).                             |     Нет     |
| `DB_FILENAME`          | Имя файла базы данных SQLite (по умолчанию `users.db`).                         |     Нет     |
| `MODEL_NAME`           | Модель Whisper: `tiny`, `base`, `small`, `medium`, `large`, `turbo`.            |     Нет     |
| `TRANSCRIPTION_DEVICE` | Устройство для обработки: `cpu` или `cuda`.                                     |     Нет     |
| `UPDATE_TIME_POLICY`   | Когда обновлять лимиты: `BEFORE` или `AFTER` транскрибации.                     |     Нет     |
| `RESET_SCHEDULE`       | Сброс лимитов: `DAILY`, `WEEKLY`, `MONTHLY`.                                    |     Нет     |
| `LOG_LEVEL`            | Уровень логирования: `INFO`, `DEBUG`, `WARNING`, `ERROR`.                       |     Нет     |

## 🚦 Управление ботом

-   **Просмотр логов:** `docker-compose logs -f`
-   **Остановка бота:** `docker-compose down`
-   **Перезапуск бота:** `docker-compose restart`

## 📄 Лицензия

Этот проект лицензирован под лицензией MIT.
