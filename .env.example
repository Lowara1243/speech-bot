# Ваш токен для Telegram-бота, полученный от @BotFather.
BOT_TOKEN=12345:your_telegram_bot_token

# Ваш личный Telegram User ID для получения неограниченных лимитов
ADMIN_ID=123456789

# Имя файла базы данных SQLite. Файл будет автоматически создан в директории 'data/'.
DB_FILENAME=users.db

# Модель Whisper, которую будет использовать бот.
# Возможные значения: tiny, base, small, medium, large, turbo
MODEL_NAME=base

# Устройство для выполнения транскрибации.
# GPU значительно ускоряет обработку, но требует совместимой видеокарты NVIDIA и установленного CUDA.
# Если указан GPU, но он недоступен, бот автоматически переключится на CPU.
# Возможные значения: CPU, GPU
TRANSCRIPTION_DEVICE=CPU

# Когда списывать время из лимита пользователя.
# AFTER: после успешного распознавания (безопаснее для пользователя).
# BEFORE: сразу после получения аудио (быстрее, но спишет время даже при ошибке).
# Возможные значения: AFTER, BEFORE
UPDATE_TIME_POLICY=AFTER

# Как часто сбрасывать лимиты времени для всех пользователей.
# Возможные значения: DAILY, WEEKLY, MONTHLY
RESET_SCHEDULE=DAILY

# Уровень логирования для вывода в консоль.
# Возможные значения: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO